# RESUME addendum — 2026-07-02 PM

Supplements `notes/RESUME.md` (written 2026-07-01, before the last few hours of
work). Read both. Where they conflict, this file wins.

---

## 1. v15L3 cue interpretation — LOCKED IN

User chose **"realistic natural rest: butt just above rail (~38\"), gentle
tilt"**: full 58" cue length, whole cue shifted 12" inward, 6° tilt,
tip Z = 32" (on felt), butt Z = 38", `INSET_FROM_RAIL = 16`. Already
implemented in `build_pool_room_furniture.py` (~line 2144). Do not re-derive.

## 2. Fixed orientation rule — DO NOT re-derive

- image-top = high Y = **SOUTH** (Main Entry east end, Emergency Exit east half)
- image-bottom = low Y = **NORTH** (stage, lockers, Storage A)
- image-right = high X = **EAST**; image-left = low X = **WEST**

## 3. Annotator pixel-coord gotcha

Rendered PNG is 2560×1440 but preview reads show ~1430×810; multiply
visually-estimated coords by ~1.78 for real placement. Corrected
`me_door=(1110, 880)` works (for the 8 ft looking-at-ME render).

## 4. Persistent user instructions

- Always annotate renders similar to the initial 2D diagram.
- Strip ALL numeric position coords from annotation callouts — descriptive
  only (rule since v15).

## 5. v15c door positions — DO NOT change

- Main Entry: E wall, south (ROOM_L-70, 70)
- Kitchen: E wall (290, 40)
- Emergency Exit: S wall (ROOM_W-95, 65)
- Storage A: N wall, east end (ROOM_W-40, 36)
- Storage B: E wall, north end (4, 36)

## 6. Room constants

ROOM_W=316", ROOM_L=682", IN=0.0254, TBL_H=32", RAIL_H=3.5", BEAM_Y=332".
Main Entry door center (316, 612). Emergency Exit door center (221, 682).

## 7. Pending render spec (the interrupted task)

Copy `drivers/driver_v15L3_looking_at_ME.py` → `driver_v15L3_looking_at_ME_5p5ft.py`,
change `EYE_H = 96` → `EYE_H = 66`, output filename includes `_5p5ft`.
Camera at x=60, y=652 aiming at Main Entry (x=316, y=612, z=48), 14mm lens,
2560×1440 @ 24 samples, Filmic Medium High Contrast, Cycles CPU, denoise on.
**Must inspect the image before annotating** — the door's pixel location
shifts when the camera drops from 8 ft to 5.5 ft.

## 8. Blender run pattern

Binary at `/tmp/blender-4.2.3-linux-x64/blender`, staging at
`/tmp/blender_test/` (may reset on sandbox reboot). Invoke with
`setsid nohup ... & disown` (or the harness's background-task mechanism).
2560×1440 @ 24 samples ≈ 11–12 min single, ~22 min for parallel pairs.

## 9. Nightly Drive mirror cron (Perplexity environment only)

Cron ID `87e306b5`, `0 9 * * *` UTC (= 2:00 AM PDT), owned by Perplexity
session `f5079d24-e928-44f1-b530-e5d06be45d5e`. Was failing on
`export_files` returning `requires_auth=true` (Drive auth-scope loop,
diagnostic `f5079d24`). Options: switch mirror to Dropbox (also connected
there) or reconnect Drive. **Not manageable from non-Perplexity
environments** — a Claude Code session cannot see or fix this cron.

## 10. Sharing convention (Perplexity environment only)

`share_file(name="pool_room_layout", should_validate=False)` for the main
asset's version history.

## 11. Baseline zip

`pool_room_blender_2026-07-01.zip` (10.4 MB) shared earlier; Perplexity asset
ID `04401031-549a-4a9e-bfe9-0660445be00a`.

## 12. Prior session canonical URL

https://www.perplexity.ai/computer/tasks/97d989a0-0edd-4fd4-a18a-d7c9b51a0ebe

---

## 2026-07-02 PM takeover (Claude Code remote session)

- Repo cloned at `fb4515a` to `/workspace/pool-room-blender` in a Claude Code
  remote container. Blender 4.2.3 downloaded fresh from
  download.blender.org (the container has no Perplexity connectors,
  `share_file`, or `pplx-tool`; GitHub access goes through the session's
  git proxy / GitHub MCP tools).
- Item 7's driver has been created as
  `drivers/driver_v15L3_looking_at_ME_5p5ft.py`.
- **Item 7 is DONE** (commit `e948bb7`): rendered, inspected, annotated via
  `annotators/annotate_v15L3_west_to_ME_5p5ft.py`; both PNGs in `renders/`.
  Door pixel anchor at 5.5 ft is (1145, 820) — it was (1110, 880) at 8 ft.
- **New gap found and fixed** (commit `992d55d`): the 21 runtime texture
  PNGs (felt/vct/plank/cmu/ceiling/panel/beam × albedo/normal/roughness)
  had only ever lived in the first sandbox's `/tmp` — never committed —
  so post-reset renders came out magenta. They are now regenerated
  procedurally (`textures/generate_textures.py`, deterministic seed),
  committed via LFS in `textures/`, and `render.sh` stages them to `/tmp`
  automatically (regenerating if the clone only has LFS pointer files).
